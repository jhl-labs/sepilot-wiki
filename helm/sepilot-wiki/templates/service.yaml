apiVersion: v1
kind: Service
metadata:
  name: {{ include "sepilot-wiki.fullname" . }}
  labels:
    {{- include "sepilot-wiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sepilot-wiki.selectorLabels" . | nindent 4 }}
