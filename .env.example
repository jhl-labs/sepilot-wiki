# SEPilot Wiki - 환경 변수 설정
# 이 파일을 .env.local로 복사한 후 실제 값을 입력하세요.

# ===========================================
# 빌드 모드 설정
# ===========================================
# static: GitHub Pages용 정적 빌드
# standalone: Kubernetes/Docker 배포용 서버 빌드 (기본값)
BUILD_MODE=standalone

# 인증 모드 설정
# public: 인증 없이 공개 접근 (GitHub Pages용)
# private: Keycloak OAuth 인증 필요 (Kubernetes용)
AUTH_MODE=public

# 클라이언트에서 인증 모드 확인용 (AUTH_MODE와 동일한 값으로 설정)
NEXT_PUBLIC_AUTH_MODE=public

# ===========================================
# Next.js 공개 환경 변수
# ===========================================
# Base path (GitHub Pages: /sepilot-wiki, 서버: 빈 문자열)
NEXT_PUBLIC_BASE_PATH=

# ===========================================
# Keycloak OAuth 설정 (AUTH_MODE=private일 때 필요)
# ===========================================
# Keycloak 클라이언트 ID
KEYCLOAK_CLIENT_ID=sepilot-wiki

# Keycloak 클라이언트 시크릿 (confidential client의 경우)
KEYCLOAK_CLIENT_SECRET=

# Keycloak 발급자 URL (realm 포함)
# 예: https://auth.example.com/realms/sepilot
KEYCLOAK_ISSUER=

# ===========================================
# NextAuth.js 설정
# ===========================================
# 앱의 공개 URL
# 예: https://wiki.example.com
NEXTAUTH_URL=http://localhost:3000

# 암호화용 시크릿 키 (openssl rand -base64 32로 생성)
NEXTAUTH_SECRET=

# ===========================================
# GitHub API 설정 (선택사항)
# ===========================================
# GitHub API 토큰 (선택사항)
# - 공개 저장소의 경우 없어도 동작하지만, API rate limit이 적용됩니다.
# - 비공개 저장소나 높은 API 사용량이 필요한 경우 설정하세요.
# - 토큰 생성: https://github.com/settings/tokens
GITHUB_TOKEN=

# GitHub 저장소 정보 (owner/repo 형식)
GITHUB_REPO=your-username/your-wiki-repo

# GitHub Enterprise Server (GHES) 설정 (선택사항)
# GITHUB_BASE_URL=https://github.mycompany.com
# GITHUB_API_URL=https://github.mycompany.com/api/v3
# GITHUB_RAW_URL=https://raw.github.mycompany.com
