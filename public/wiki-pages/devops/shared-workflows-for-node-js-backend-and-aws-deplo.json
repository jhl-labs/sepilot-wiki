{
  "title": "Shared Workflows for Node.js Backend and AWS Deployments",
  "slug": "devops/shared-workflows-for-node-js-backend-and-aws-deplo",
  "content": "\n## 소개\n이 문서는 **Node.js 백엔드**와 **AWS 인프라**를 대상으로 하는 **공유 워크플로우(Shared Workflows)** 를 설계·운용하는 방법을 안내합니다.  \n대상 독자는 다음과 같습니다.\n\n- 여러 팀·레포지토리를 보유한 조직의 개발·운영 엔지니어\n- GitHub Actions 를 사용해 CI/CD 파이프라인을 구축하려는 개발자\n- Terraform 으로 인프라를 코드화(IaC)하고 AWS ECS/ECR 로 배포하고자 하는 팀\n\n공유 워크플로우를 도입하면 **파이프라인 중복 제거**, **보안·운영 일관성 확보**, **유지보수 비용 절감** 효과를 기대할 수 있습니다 [출처](https://euno.news/posts/ko/shared-workflows-minha-experincia-definindo-pipeli-2dc274).\n\n## 배경 및 동기\n| 문제 | 현상 | 공유 워크플로우 도입 효과 |\n|------|------|--------------------------|\n| 파이프라인 중복 | 각 레포마다 lint·test·build·deploy 를 별도 구현 | 재사용 가능한 워크플로우 하나로 모든 레포 호출 |\n| 보안·운영 일관성 부족 | 권한·시크릿 관리가 레포마다 상이 | 중앙 레포에서 최소 권한 원칙 적용·통합 관리 |\n| 유지보수 비용 상승 | 파이프라인 로직 변경 시 다수 레포 수정 필요 | 워크플로우 버전 관리·롤백으로 한 곳만 업데이트 |\n\n## 공유 워크플로우 아키텍처 개요\n### 재사용 구조 (`workflow_call`)\nGitHub Actions 의 **`on: workflow_call`** 을 활용해 **중앙 레포**에 정의된 워크플로우를 다른 레포에서 호출합니다 [GitHub Docs](https://docs.github.com/en/actions/using-workflows/reusing-workflows).\n\n### 책임 별 워크플로우 분리\n| 워크플로우 | 주요 역할 | 파일명 |\n|------------|----------|--------|\n| `shared-backend-ci.yml` | Lint, 의존성 설치, 캐시, 유닛·통합 테스트, 보안 스캔 (`yarn audit`) | `.github/workflows/shared-backend-ci.yml` |\n| `shared-terraform-ci.yml` | Terraform `fmt`, `init`, `plan` (및 `checkov` 보안 검증) | `.github/workflows/shared-terraform-ci.yml` |\n| `shared-backend-deploy-ecs.yml` | Docker 이미지 빌드·ECR 푸시, ECS Blue/Green 배포 | `.github/workflows/shared-backend-deploy-ecs.yml` |\n| `shared-release-workflow.yml` | Release 브랜치 자동 생성, `main`에 PR 생성·버전 태깅 | `.github/workflows/shared-release-workflow.yml` |\n\n### 중앙 레포·버전 관리 전략\n- **전용 레포**(예: `org/shared-workflows`)에 위 파일들을 보관\n- **태그**(`@v1`, `@v2` 등)로 버전 관리 → 소비자 레포는 `uses: org/shared-workflows/.github/workflows/shared-backend-ci.yml@v1` 형태로 호출\n- 버전 업데이트 시 **릴리즈 노트**와 **마이그레이션 가이드** 제공\n\n## CI 워크플로우 (`shared-backend-ci`)\n- **입력 파라미터**  \n  - `working_directory` (예: `app`)  \n  - `node_version` (예: `'20'`)  \n  - `enable_security_scan` (boolean)  \n\n- **주요 단계**  \n    runs-on: ubuntu-latest  \n    steps:  \n        - uses: actions/checkout@v3  \n        - name: Set up Node.js  \n          uses: actions/setup-node@v3  \n          with:  \n              node-version: ${{ inputs.node_version }}  \n        - name: Cache node_modules  \n          uses: actions/cache@v3  \n          with:  \n              path: ${{ inputs.working_directory }}/node_modules  \n              key: ${{ runner.os }}-node-${{ hashFiles('${{ inputs.working_directory }}/package-lock.json') }}  \n        - name: Install dependencies  \n          run: npm ci --prefix ${{ inputs.working_directory }}  \n        - name: Lint  \n          run: npm run lint --prefix ${{ inputs.working_directory }}  \n        - name: Test (unit & integration)  \n          run: npm test --prefix ${{ inputs.working_directory }}  \n        - name: Security scan (yarn audit)  \n          if: ${{ inputs.enable_security_scan }}  \n          run: yarn audit --cwd ${{ inputs.working_directory }}  \n\n- **시크릿**  \n  - `GITHUB_TOKEN` (자동 제공)  \n\n## 인프라 CI 워크플로우 (`shared-terraform-ci`)\n- **입력 파라미터**  \n  - `tf_working_dir` (Terraform 코드 위치)  \n  - `tf_backend_bucket` (상태 백엔드 S3 버킷)  \n  - `tf_var_file` (변수 파일 경로)  \n\n- **주요 단계**  \n    runs-on: ubuntu-latest  \n    steps:  \n        - uses: actions/checkout@v3  \n        - name: Setup Terraform  \n          uses: hashicorp/setup-terraform@v2  \n        - name: Terraform fmt  \n          run: terraform fmt -check -recursive ${{ inputs.tf_working_dir }}  \n        - name: Terraform init  \n          run: terraform init -backend-config=\"bucket=${{ inputs.tf_backend_bucket }}\" ${{ inputs.tf_working_dir }}  \n        - name: Terraform plan  \n          run: terraform plan -var-file=${{ inputs.tf_var_file }} ${{ inputs.tf_working_dir }}  \n        - name: Security check with Checkov  \n          uses: bridgecrewio/checkov-action@v12  \n          with:  \n              directory: ${{ inputs.tf_working_dir }}  \n\n- **시크릿**  \n  - `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` (OIDC 연동 시 필요 시)  \n\n## CD 워크플로우 (`shared-backend-deploy-ecs`)\n- **입력 파라미터**  \n  - `environment` (`staging` | `production`)  \n  - `image_tag` (Docker 이미지 태그)  \n  - `ecr_repository` (ECR 리포지터리 URI)  \n\n- **주요 단계**  \n    runs-on: ubuntu-latest  \n    permissions:  \n        contents: read  \n        id-token: write   # OIDC for AWS IAM role  \n    steps:  \n        - uses: actions/checkout@v3  \n        - name: Login to Amazon ECR  \n          uses: aws-actions/amazon-ecr-login@v2  \n        - name: Build Docker image  \n          run: |  \n              docker build -t ${{ inputs.ecr_repository }}:${{ inputs.image_tag }} .  \n        - name: Push to ECR  \n          run: |  \n              docker push ${{ inputs.ecr_repository }}:${{ inputs.image_tag }}  \n        - name: Deploy to ECS (Blue/Green)  \n          uses: aws-actions/amazon-ecs-deploy-task-definition@v1  \n          with:  \n              task-definition: ecs-task-def.json  \n              service: ${{ inputs.environment }}-service  \n              cluster: ${{ inputs.environment }}-cluster  \n              wait-for-service-stability: true  \n\n- **시크릿**  \n  - `AWS_ROLE_TO_ASSUME` (GitHub OIDC 연동용 IAM 역할 ARN)  \n\n## 릴리즈 워크플로우 (`shared-release-workflow`)\n- **입력 파라미터**  \n  - `release_branch_prefix` (기본값: `release/`)  \n  - `main_branch` (기본값: `main`)  \n\n- **주요 단계**  \n    runs-on: ubuntu-latest  \n    steps:  \n        - uses: actions/checkout@v3  \n        - name: Create release branch  \n          run: |  \n              git checkout -b ${{ inputs.release_branch_prefix }}$(date +%Y.%m.%d) ${{ inputs.main_branch }}  \n              git push origin HEAD  \n        - name: Open PR to main  \n          uses: peter-evans/create-pull-request@v5  \n          with:  \n              branch: ${{ inputs.release_branch_prefix }}$(date +%Y.%m.%d)  \n              base: ${{ inputs.main_branch }}  \n              title: \"Release $(date +%Y.%m.%d)\"  \n              body: \"Automated release PR generated by shared-release-workflow.\"  \n        - name: Tag version  \n          run: |  \n              VERSION=$(date +%Y.%m.%d)  \n              git tag -a v$VERSION -m \"Release $VERSION\"  \n              git push origin v$VERSION  \n\n- **시크릿**  \n  - `GITHUB_TOKEN` (자동 제공)  \n\n## 워크플로우 래퍼 예시\n소비자 레포에서 **CI → Deploy → Release** 를 순차적으로 호출하는 래퍼 워크플로우 예시입니다.\n\n    name: CI → Deploy → Release\n    on:\n      push:\n        branches: [ main ]\n    jobs:\n      ci:\n        uses: org/shared-workflows/.github/workflows/shared-backend-ci.yml@v1\n        with:\n          working_directory: app\n          node_version: '20'\n          enable_security_scan: true\n      deploy:\n        needs: ci\n        uses: org/shared-workflows/.github/workflows/shared-backend-deploy-ecs.yml@v1\n        with:\n          environment: staging\n          ecr_repository: 123456789012.dkr.ecr.us-east-1.amazonaws.com/my-app\n          image_tag: ${{ github.sha }}\n      release:\n        needs: deploy\n        if: ${{ needs.deploy.result == 'success' }}\n        uses: org/shared-workflows/.github/workflows/shared-release-workflow.yml@v1\n\n## 권한 및 시크릿 관리\n- **최소 권한 원칙**에 따라 각 작업에 필요한 권한만 부여합니다. 예: Lint/Test 단계는 `contents: read` 만 필요하고, Deploy 단계는 `id-token: write` 와 `secrets: write` 가 추가로 필요합니다 [GitHub 권한 가이드](https://docs.github.com/en/actions/using-workflows/using-permissions-for-github-actions).\n- **GitHub OIDC** 를 활용해 AWS IAM 역할을 임시로 가정합니다. 이를 통해 **시크릿을 레포에 저장하지 않고** 안전하게 AWS 리소스에 접근할 수 있습니다 [AWS OIDC 연동 가이드](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_create_oidc.html).\n- **시크릿 명명 규칙** 예시  \n  - `AWS_ROLE_TO_ASSUME` – 배포 시 사용할 IAM 역할 ARN  \n  - `GITHUB_TOKEN` – 자동 제공, 별도 설정 불필요  \n\n## 파라미터화 및 다중 프로젝트 지원\n- `working_directory`, `app_path`, `image_tag` 등 **입력 파라미터**를 통해 **모노레포** 혹은 **다양한 디렉터리 구조**를 가진 프로젝트에서도 동일 워크플로우 재사용이 가능합니다.\n- 예시: 모노레포에서 `service-a` 와 `service-b` 가 각각 다른 `working_directory` 를 지정해 호출하면 동일 파일을 공유하면서도 독립적인 파이프라인을 운영할 수 있습니다.\n\n## 테스트·검증 도구 체인\n| 도구 | 목적 | 적용 위치 |\n|------|------|-----------|\n| `actionlint` | 워크플로우 문법·베스트 프랙티스 검사 | PR 검증 단계 (예: `peter-evans/actionlint@v2`) |\n| `shellcheck` | 쉘 스크립트 정적 분석 | CI 단계 스크립트 검증 |\n| `checkov` | Terraform 보안·정책 검사 | `shared-terraform-ci.yml` 의 `checkov-action` 사용 |\n\n- **로컬 테스트**: `act` CLI 로 GitHub Actions 를 로컬에서 실행해 회귀 테스트 수행 가능 [act GitHub](https://github.com/nektos/act).\n\n## 문서화·채택 가이드\n1. **README**에 다음 항목 포함  \n   - 워크플로우 호출 예시 (위 래퍼 예시)  \n   - 필요 시크릿 목록 및 권한 설정 방법  \n   - 버전 태그(`@v1`) 사용법 및 업데이트 절차  \n2. **파일럿 단계**  \n   - 1~2개의 대표 레포에 적용 후 피드백 수집  \n   - 파라미터 기본값 조정 및 문서 보강  \n3. **조직 차원 채택**  \n   - 중앙 레포에 `CHANGELOG.md` 유지  \n   - 버전 업그레이드 시 **자동 알림**(예: Slack) 설정  \n\n## 유지·보수 전략\n- **버전 관리 정책**: 메이저 변경 시 `v2` 태그 생성, 마이너·패치 변경은 `v1.x` 로 관리. 각 릴리즈에 **마이그레이션 가이드** 포함.\n- **롤백·핫픽스**: 이전 태그(`@v0.9`) 로 바로 호출 가능하도록 설계. 긴급 수정 시 `hotfix` 브랜치를 만들어 `v1.0.1-hotfix` 로 배포.\n- **기여 흐름**: 내부 팀은 Pull Request 로 워크플로우 파일을 수정하고, **CODEOWNERS** 로 리뷰어 지정. 외부 기여자는 별도 레포 포크 후 PR 제출 가능.\n\n## FAQ\n**Q1. 워크플로우를 커스터마이징하고 싶어요.**  \nA. `with:` 섹션에 새로운 입력 파라미터를 추가하고, 워크플로우 파일 내 `inputs.<name>` 로 참조하면 됩니다. 기존 파라미터와 호환성을 유지하려면 기본값을 지정하세요.\n\n**Q2. 비공개 레포에서 공유 워크플로우를 호출할 수 있나요?**  \nA. 네. 호출 레포와 공유 워크플로우 레포가 동일 조직에 속해 있으면 `workflow_call` 로 접근 가능합니다. 조직 외부 레포에서는 **퍼블릭** 레포로 전환하거나 **GitHub Marketplace** 에서 배포해야 합니다.\n\n**Q3. 비용 최적화와 배포 전략 선택은 어떻게 해야 하나요?**  \nA. **Staging** 환경에서는 `blue/green` 대신 `rolling` 배포를 사용해 비용을 절감할 수 있습니다. `shared-backend-deploy-ecs.yml` 에 `deployment_strategy` 입력 파라미터를 추가하고, 값에 따라 `aws-actions/amazon-ecs-deploy-task-definition` 옵션을 조정하면 됩니다.\n\n## 참고 자료·링크\n- 원본 블로그 포스트: *Shared Workflows: 재사용 가능한 파이프라인을 정의한 나의 경험* – [euno.news](https://euno.news/posts/ko/shared-workflows-minha-experincia-definindo-pipeli-2dc274)  \n- GitHub Actions 재사용 워크플로우 공식 문서: <https://docs.github.com/en/actions/using-workflows/reusing-workflows>  \n- AWS OIDC 연동 가이드: <https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_create_oidc.html>  \n- Terraform 보안 검사 `checkov` 공식 페이지: <https://www.checkov.io/>  \n- `actionlint` 사용법: <https://github.com/rhysd/actionlint>  \n\n---",
  "lastModified": "2026-03-13T12:12:52Z",
  "author": "SEPilot AI",
  "status": "published",
  "isDraft": false,
  "isInvalid": false,
  "tags": [
    "GitHub Actions",
    "CI/CD",
    "Node.js",
    "AWS",
    "Terraform",
    "ECS",
    "Shared Workflows"
  ],
  "history": [
    {
      "sha": "6b5a1b5",
      "message": "chore: Issue Processor 실행 결과",
      "author": "GitHub Action",
      "authorEmail": "action@github.com",
      "date": "2026-03-13T12:12:52Z",
      "isAutoCommit": false,
      "additions": 0,
      "deletions": 0
    }
  ]
}