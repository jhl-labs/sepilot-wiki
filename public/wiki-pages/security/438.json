{
  "title": "Security/438",
  "slug": "security/438",
  "content": "---\ntitle: 자동 감지: 뉴스 인텔리전스 (euno.news) – SQL Injection UNION 실습 가이드\nauthor: SEPilot AI\nstatus: draft\ntags: [SQL Injection, UNION, PortSwigger Lab, Burp Suite, 웹 보안]\nquality_score: 73\n---\n\n## 1. 문서 개요\n**목적** – PortSwigger에서 제공하는 “SQL Injection – UNION attack, retrieving data from other tables” 실습을 단계별로 재현하고, 취약점 이해 및 방어 방법을 정리한다.  \n**대상 독자** – 웹 보안 엔지니어, 침투 테스트 담당자, 보안 교육 담당자 및 보안에 관심 있는 개발자.  \n**주요 학습 포인트**  \n- 반환 컬럼 수와 텍스트 컬럼 식별 방법  \n- UNION 기반 페이로드 구성 및 Burp Suite를 이용한 주입 절차  \n- 추출된 인증 정보 활용 및 권한 상승 시나리오  \n- 실무 적용 가능한 방어 권고사항  \n\n## 2. 실습 환경 및 사전 준비\n- **Lab URL**: `https://portswigger.net/web-security/learning-paths/sql-injection/sql-injectio` (PortSwigger 공식 실습 페이지)【euno.news】  \n- **접근 방법**: 브라우저에서 Lab 페이지에 접속 후 “Start Lab” 버튼을 눌러 테스트 환경에 진입한다.  \n- **사용 도구**  \n  - 웹 브라우저 (Chrome, Firefox 등)  \n  - Burp Suite (Community 혹은 Professional) – 프록시 설정 후 요청 변조에 활용  \n- **기본 인증 흐름**  \n  - 로그인 폼에 이메일과 비밀번호를 입력 → 정상적인 인증 시 메인 페이지로 이동  \n  - 실습에서는 인증 우회를 위해 SQL Injection을 사용한다.  \n- **테스트 계정**: Lab에 기본 제공되는 계정(예: `wiener` / `peter`)은 실습에 직접 사용되지 않으며, 공격 목표는 **관리자 계정**의 `username`과 `password`를 추출하는 것이다.\n\n## 3. 취약점 개요\n- **취약점 유형**: SQL Injection – UNION 기반 데이터 추출  \n- **취약한 파라미터**: 로그인 폼의 `email` 혹은 `password` 입력값이 직접 SQL 문자열에 삽입되는 구조(구체적인 쿼리문은 공개되지 않음)  \n- **공격 목표**: `users` 테이블(또는 유사 테이블)에서 모든 사용자명과 비밀번호를 추출하고, 이를 이용해 관리자 계정에 로그인한다【euno.news】  \n\n## 4. 공격 전 단계: 정보 수집\n### 4‑1. 컬럼 수 파악\n1. **ORDER BY 기법** – `email` 파라미터에 `ORDER BY 1--` 와 같이 삽입해 오류가 발생하지 않으면 최소 1개의 컬럼 존재 확인.  \n2. **UNION SELECT NULL** 시도 – `email='test' UNION SELECT NULL--` 로 요청을 보내고, 오류가 발생하면 컬럼 수가 부족함을 의미한다.  \n3. 위 과정을 반복해 **2개의 컬럼**이 정상적으로 반환됨을 확인한다. (Lab에서는 두 컬럼 모두 텍스트 데이터를 포함)【euno.news】\n\n### 4‑2. 텍스트 컬럼 식별\n- `UNION SELECT 'a','b'` 를 주입하여 응답 페이지에 `a`와 `b`가 그대로 출력되는지 확인한다.  \n- 두 컬럼 모두 문자열을 표시하므로, **두 컬럼 모두 텍스트 컬럼**으로 활용 가능함을 확인한다【euno.news】.\n\n## 5. 텍스트 데이터를 포함하는 컬럼 식별\n- 위 단계에서 확인한 바와 같이 `col1`과 `col2`가 텍스트를 출력하므로, 실제 데이터(예: `username`, `password`)를 반환하도록 교체하면 된다.  \n- 컬럼 순서는 원본 쿼리와 동일해야 하며, 순서를 바꾸면 오류가 발생한다.\n\n## 6. UNION 페이로드 구성 및 주입\n### 6‑1. 기본 페이로드 형식\n```\n' UNION SELECT username, password FROM users--\n```\n- 앞뒤에 작은 따옴표(`'`)와 주석(`--`)을 사용해 기존 쿼리와 결합한다.  \n\n### 6‑2. Burp Suite를 이용한 변조 절차\n1. Burp Suite 프록시를 활성화하고 브라우저와 연결한다.  \n2. 로그인 폼에 임의의 값(예: `a`)을 입력하고 “Login” 버튼을 클릭한다.  \n3. **Proxy → HTTP history**에서 해당 POST 요청을 찾아 **Intercept**를 켜고, `email` 파라미터 값을 위 페이로드로 교체한다.  \n4. 요청을 전송하고 응답 페이지에 **관리자 계정의 사용자명과 비밀번호**가 표출되는지 확인한다.  \n\n### 6‑3. 성공적인 데이터 추출 시나리오\n- 응답에 `admin` 과 해당 `password` 문자열이 나타나면, 이를 메모한다.  \n- 추출된 비밀번호를 사용해 로그인 폼에 `admin` / `<추출된 비밀번호>` 를 입력하면 관리자 권한으로 로그인한다.\n\n## 7. 추출된 데이터 활용\n- **관리자 로그인**: 정상적인 인증 절차를 우회해 관리자 대시보드에 접근한다.  \n- **세션 쿠키 확보**: 로그인 후 발급되는 세션 쿠키(`session=...`)를 복사해 Burp Suite 혹은 브라우저 개발자 도구에서 재사용하면 지속적인 권한을 유지할 수 있다.  \n- **권한 상승 확인**: 관리자 전용 페이지(예: 사용자 관리, 설정 변경 등)에 접근 가능한지 확인한다.\n\n## 8. 방어 및 보안 권고\n| 권고사항 | 설명 |\n|---|---|\n| **파라미터화된 쿼리 / Prepared Statement** | 사용자 입력을 SQL 문에 직접 삽입하지 않고 바인딩 변수로 처리한다. |\n| **서버‑사이드 입력 검증** | 화이트리스트 기반 검증을 적용해 허용된 형식(예: 이메일 정규식)만 통과하도록 한다. |\n| **입력값 이스케이프 및 정제** | 특수 문자(`'`, `\"`, `--` 등)를 적절히 이스케이프하거나 제거한다. |\n| **최소 권한 원칙** | 애플리케이션 계정에 DB 접근 권한을 최소화하고, `SELECT` 권한만 부여한다. |\n| **에러 메시지 숨김** | DB 오류가 사용자에게 노출되지 않도록 일반적인 오류 페이지를 반환한다. |\n| **보안 로그 모니터링** | 비정상적인 `UNION SELECT` 패턴을 탐지해 알림을 발생시킨다. |\n\n## 9. 교훈 및 베스트 프랙티스\n- **컬럼 수 파악**은 UNION 기반 인젝션의 첫 번째 관문이며, `ORDER BY`와 `UNION SELECT NULL` 조합이 가장 직관적이다.  \n- **텍스트 컬럼 식별**을 통해 실제 데이터가 반환될 수 있는 위치를 정확히 찾는 것이 성공적인 데이터 추출의 핵심이다.  \n- 실습을 통해 **입력 검증 부재**가 얼마나 쉽게 권한 상승으로 이어지는지 체감할 수 있다. 교육 현장에서 이러한 시나리오를 직접 수행하면 보안 인식 제고에 큰 도움이 된다.\n\n## 10. 참고 자료\n- **PortSwigger Lab 공식 문서** – “SQL Injection – UNION attack, retrieving data from other tables”【euno.news】  \n- **euno.news 원문 기사** – 실습 요약 및 단계별 설명【euno.news】  \n- **OWASP SQL Injection Prevention Cheat Sheet** – 파라미터화와 입력 검증 권고사항 (공식 OWASP 문서) https://owasp.org/www-project-cheat-sheets/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html  \n- **Burp Suite 공식 문서** – 프록시 설정 및 요청 변조 방법 https://portswigger.net/burp/documentation/desktop  \n\n## 11. 부록\n### 11‑1. 주요 페이로드 목록\n- 컬럼 수 탐지: `email='test' UNION SELECT NULL--`  \n- 컬럼 수 2 확인: `email='test' UNION SELECT NULL,NULL--`  \n- 텍스트 컬럼 확인: `email='test' UNION SELECT 'a','b'--`  \n- 데이터 추출: `email='test' UNION SELECT username,password FROM users--`  \n\n### 11‑2. Burp Suite 설정 (텍스트 설명)\n1. **Proxy → Options** → “Intercept client requests” 활성화  \n2. **Proxy → Intercept** → “Intercept is on” 상태 유지  \n3. 요청 수정 시 **Edit** 창에서 파라미터 값을 위 페이로드로 교체 후 “Forward”  \n\n### 11‑3. 컬럼 수 탐지용 SQL 템플릿\n```\n' UNION SELECT {col1},{col2},...,{colN} FROM {table}--\n```\n- `{col1}`~`{colN}` 은 `NULL` 혹은 임시 문자열(`'a'`) 로 채워 컬럼 수를 맞춘다.  \n\n### 11‑4. 용어 정의\n- **SQL Injection**: 외부 입력이 적절히 검증되지 않아 SQL 구문에 삽입되는 취약점.  \n- **UNION**: 두 개 이상의 SELECT 결과를 하나의 결과 집합으로 결합하는 SQL 연산자.  \n- **Burp Suite**: 웹 애플리케이션 보안 테스트를 위한 프록시 및 스캐너 도구.  \n\n---",
  "lastModified": "2026-04-11T23:47:29Z",
  "author": "GitHub Action",
  "status": "draft",
  "isDraft": true,
  "isInvalid": false,
  "tags": [],
  "history": [
    {
      "sha": "f862656",
      "message": "chore: 대시보드 통계 수집 - 2026-04-11 23:47",
      "author": "GitHub Action",
      "authorEmail": "action@github.com",
      "date": "2026-04-11T23:47:29Z",
      "isAutoCommit": false,
      "additions": 0,
      "deletions": 0
    }
  ]
}