{
  "title": "React 18 Concurrent Rendering 및 Suspense 기반 선언형 데이터 페칭 가이드",
  "slug": "frontend/react-concurrent-rendering",
  "content": "\n## 1. 서론\n- **문서 목적**  \n  React 18에서 제공되는 Concurrent Rendering, Suspense, 그리고 현재 공식 API(`useTransition`, `useDeferredValue`, `useSyncExternalStore`)를 활용한 선언형 데이터 페칭 패턴을 소개하고, 기존 `useEffect` 기반 코드와의 마이그레이션 방법을 제시한다.  \n- **대상 독자**  \n  프론트엔드 개발자, 팀 리드, React 18 이상으로 마이그레이션을 고려 중인 프로젝트 관리자.  \n- **핵심 개념 요약**  \n  - **Concurrent Rendering**: React Fiber가 작업을 중단·재시작하면서 UI 일관성을 유지하는 렌더링 모델.  \n  - **Suspense**: 비동기 작업이 `Promise`를 throw 하면 가장 가까운 `<Suspense>` 경계가 fallback UI를 자동으로 표시한다.  \n  - **선언형 데이터 페칭(Concept)**: 아직 공식 API는 아니지만, `useTransition`·`useDeferredValue`와 결합해 **데이터 로딩을 선언형으로 표현**할 수 있다.  \n  - **useSyncExternalStore**: 외부 스토어(예: Redux, Zustand)와의 동기화에 최적화된 Hook으로, 서버‑사이드 렌더링(SSR)과 Concurrent 모드 모두에서 안전하게 사용할 수 있다.  \n\n> 현재 React 19는 출시되지 않았으며, `@use`·`useActionState` 같은 API는 공식에 존재하지 않는다. 이 문서에서는 해당 개념을 **예정 기능** 혹은 **커뮤니티 실험** 수준으로 명시한다.  \n\n## 2. React 18 핵심 변화 개관\n- **Concurrent Mode 기본 활성화**  \n  React 18부터는 `createRoot`를 사용하면 Concurrent Rendering이 기본으로 활성화된다. 별도 플래그가 필요하지 않다. ([React Docs – Concurrent Rendering](https://reactjs.org/docs/concurrent-mode-intro.html))  \n- **새로운 공식 Hook**  \n  - `useTransition` – UI 업데이트와 데이터 로딩을 분리해 사용자 경험을 부드럽게 만든다.  \n  - `useDeferredValue` – 값이 변할 때마다 렌더링을 지연시켜 불필요한 재렌더링을 방지한다.  \n  - `useSyncExternalStore` – 외부 스토어와의 동기화에 최적화된 API이며, SSR에서도 일관된 결과를 보장한다.  \n- **Suspense for Data Fetching (Experimental)**  \n  React 18에서는 `React.lazy`와 `Suspense`를 사용해 코드 스플리팅을, `use`와 같은 데이터 페칭 Hook은 아직 실험 단계이다. 공식 문서는 **Experimental** 라벨을 붙이고 있다.  \n\n## 3. Concurrent Rendering 이해\n- **Scheduler와 Fiber 구조**  \n  - Scheduler는 작업 우선순위를 판단하고, 높은 우선순위 작업이 들어오면 현재 작업을 **중단**한다.  \n  - Fiber는 각 컴포넌트 인스턴스를 트리 형태로 표현하며, 중단된 작업은 `Suspended` 플래그가 설정된다.  \n- **동시성 제어 흐름**  \n  1. `createRoot` 로 앱을 마운트 → Concurrent Mode 활성화.  \n  2. UI 업데이트가 발생하면 Scheduler가 작업을 작은 단위로 나눈다.  \n  3. 높은 우선순위 입력(예: 클릭) 발생 시 현재 작업을 **중단**하고, 입력을 먼저 처리한다.  \n  4. 중단된 Fiber는 상태를 보존한 채 대기열에 남아, 나중에 재개된다.  \n- **UI 일관성 보장을 위한 원칙**  \n  - **Atomic Updates**: 상태 업데이트는 전체 트리에서 원자적으로 적용된다.  \n  - **Consistency Boundaries**: `<Suspense>` 경계가 UI 일관성을 유지하는 최소 단위가 된다.  \n\n## 4. Suspense 메커니즘 심층 분석\n- **Suspense 경계와 fallback UI 동작 원리**  \n  - 컴포넌트가 아직 해결되지 않은 `Promise`를 throw 하면, 가장 가까운 `<Suspense fallback={...}>` 가 이를 잡아 fallback UI를 렌더링한다.  \n  - Promise가 해결되면 해당 Fiber가 재스케줄링되어 실제 UI가 다시 렌더링된다. ([React Docs – Suspense](https://reactjs.org/docs/concurrent-mode-suspense.html))  \n- **Promise‑throw 패턴과 Reconciler 연동**  \n  1. 컴포넌트 내부에서 `await` 대신 `throw promise` 형태로 비동기 작업을 전달.  \n  2. Reconciler가 `Promise` 상태를 확인하고 `pending`이면 `Suspended` 로 표시.  \n  3. 가장 가까운 Suspense 경계가 fallback UI를 표시.  \n  4. Promise 해결 → Reconciler가 업데이트를 커밋하고 UI를 재렌더링.  \n- **데이터 스트리밍 시나리오**  \n  - 스트리밍 API와 결합하면 데이터가 부분적으로 도착할 때마다 개별 Suspense 경계가 해제되어 점진적 UI 업데이트가 가능하다. 현재 공식 예제는 제한적이며, 커뮤니티 실험을 참고한다.  \n\n## 5. 선언형 데이터 페칭 패턴 (Concept)\n> **주의**: `@use`와 같은 Hook은 아직 공식 API가 아니다. 여기서는 `useTransition`·`useDeferredValue`와 결합해 비공식적인 선언형 패턴을 구현하는 방법을 소개한다.\n\n### 5‑1. 기본 아이디어\n1. **데이터 로딩을 Transition 으로 감싸기**  \n   ```tsx\n   const [startTransition, isPending] = useTransition();\n   const [data, setData] = useState(null);\n   \n   startTransition(() => {\n     fetchUser(id).then(setData);\n   });\n   ```\n2. **Deferred Value 로 UI 렌더링 지연**  \n   ```tsx\n   const deferredData = useDeferredValue(data);\n   return deferredData ? <UserInfo user={deferredData} /> : <Spinner />;\n   ```\n3. **Suspense와 결합**  \n   - 위 패턴을 `<Suspense>` 경계 안에 배치하면, `isPending` 상태를 fallback UI와 동기화할 수 있다.  \n\n### 5‑2. 구현 예시 (4칸 들여쓰기 코드 블록)\n    import { useState, useTransition, useDeferredValue, Suspense } from 'react';\n    \n    async function fetchUser(id) {\n      const res = await fetch(`/api/users/${id}`);\n      if (!res.ok) throw new Error('Network error');\n      return res.json();\n    }\n    \n    export function UserProfile({ id }) {\n      const [startTransition, isPending] = useTransition();\n      const [user, setUser] = useState(null);\n    \n      useEffect(() => {\n        startTransition(() => {\n          fetchUser(id).then(setUser);\n        });\n      }, [id]);\n    \n      const deferredUser = useDeferredValue(user);\n    \n      return (\n        <Suspense fallback={<div>Loading…</div>}>\n          {isPending && <div>Loading spinner...</div>}\n          {deferredUser ? (\n            <>\n              <h2>Welcome, {deferredUser.name}</h2>\n              <p>Email: {deferredUser.email}</p>\n            </>\n          ) : null}\n        </Suspense>\n      );\n    }\n\n> 위 예시는 **공식 API**(`useTransition`, `useDeferredValue`)만을 사용하므로 React 18 이상에서 바로 동작한다.  \n\n## 6. 폼 처리와 `useActionState` (Concept)\n`useActionState`는 아직 공식 API가 아니다. 현재 React 18에서는 **서버 액션**을 구현하려면 `fetch`와 `useTransition`을 조합하거나, 외부 상태 관리 라이브러리(Redux Toolkit, React Query 등)를 활용한다.\n\n### 6‑1. 비공식적인 폼 처리 패턴\n```tsx\nimport { useState, useTransition } from 'react';\n\nexport function LoginForm() {\n  const [startTransition, isPending] = useTransition();\n  const [error, setError] = useState(null);\n  \n  async function handleSubmit(e) {\n    e.preventDefault();\n    const formData = new FormData(e.target);\n    startTransition(async () => {\n      try {\n        const res = await fetch('/api/login', {\n          method: 'POST',\n          body: formData,\n        });\n        if (!res.ok) throw new Error('Login failed');\n        // 성공 시 페이지 이동 등 로직\n      } catch (err) {\n        setError(err);\n      }\n    });\n  }\n  \n  return (\n    <form onSubmit={handleSubmit}>\n      <input name=\"username\" required />\n      <input name=\"password\" type=\"password\" required />\n      <button type=\"submit\" disabled={isPending}>\n        {isPending ? 'Logging in…' : 'Login'}\n      </button>\n      {error && <p style={{color: 'red'}}>{error.message}</p>}\n    </form>\n  );\n}\n```\n- `useTransition` 으로 **비동기 제출**을 UI와 분리한다.  \n- 별도 `useState` 로 `isPending`, `error` 를 관리하므로 `useActionState`와 유사한 흐름을 구현할 수 있다.  \n\n## 7. 마이그레이션 가이드\n1. **`useEffect` 기반 로딩 코드 식별**  \n   - `useEffect(() => { fetch(...).then(setData); }, [])` 형태와 `const [loading, setLoading] = useState(true)` 패턴을 찾아낸다.  \n2. **`useTransition`·`useDeferredValue` 로 전환**  \n   - API 래퍼가 **stable Promise**를 반환하도록 수정한다.  \n   - 컴포넌트 내부에서 `startTransition(() => fetchXxx().then(setData))` 로 교체하고, 가장 가까운 `<Suspense>` 경계에 fallback UI를 추가한다.  \n3. **폼 처리 업데이트**  \n   - 기존 `onSubmit` 핸들러와 `isSubmitting` 상태를 제거하고, 위 **6‑1** 예시와 같이 `useTransition` 으로 비동기 제출을 감싸는다.  \n4. **호환성 체크리스트**  \n   - **React 버전**: 프로젝트 전체를 React 18 이상(`react@18.x`)으로 업그레이드해야 한다.  \n   - **서드파티 라이브러리**: React Router v6.4+ 의 `loader`와 `useLoaderData`는 Suspense와 자연스럽게 결합된다. ([React Router Docs](https://reactrouter.com/en/main/start/overview))  \n   - **SSR**: `useSyncExternalStore` 로 외부 스토어를 구독하면 서버와 클라이언트 간 상태 불일치를 방지한다.  \n   - **타입 정의**: `@types/react` 최신 버전에서 새로운 Hook 타입을 확인한다.  \n\n## 8. 베스트 프랙티스 및 성능 최적화\n### Suspense 경계 설계 원칙\n- **범위 최소화**: 작은 UI 영역에만 `<Suspense>` 를 적용해 로딩 UI가 전체 페이지를 가리지 않게 한다.  \n- **중첩 경계 활용**: 데이터 의존도가 다른 부분은 별도 경계로 나누어, 일부 데이터가 준비되면 즉시 표시한다.  \n\n### 데이터 캐싱 전략\n- **React Cache (Experimental)**: `unstable_createCache` 로 만든 캐시를 `use` 와 결합해 동일 Promise 재사용을 보장한다. 아직 실험 단계이므로 프로덕션에서는 React Query 또는 SWR 같은 라이브러리를 권장한다.  \n- **Stable Promise 반환**: API 함수 내부에서 `useMemo` 혹은 전역 객체에 Promise를 저장해 동일 인스턴스가 재사용되도록 설계한다.  \n\n### SSR과 Hydration 차이점\n- **SSR**: 서버에서 `<Suspense>` 경계는 `fallback` 을 즉시 렌더링한다. 클라이언트가 재수행하면서 실제 데이터가 채워진다.  \n- **Hydration**: `ReactDOM.hydrateRoot` 를 사용하면 서버에서 전달된 HTML과 클라이언트 상태가 일치하도록 보장한다. `useSyncExternalStore` 로 외부 스토어를 구독하면 hydration mismatch 를 방지한다. ([React Docs – Server Rendering](https://reactjs.org/docs/react-dom-server.html))  \n\n## 9. 테스트·디버깅 전략\n- **테스트 패턴**  \n  - `@testing-library/react` 와 `jest` 를 사용해 Suspense 내부 비동기 로직을 검증한다.  \n    ```tsx\n    await waitFor(() => expect(screen.getByText(/Welcome/)).toBeInTheDocument());\n    ```  \n  - `act` 블록 안에서 `startTransition` 을 호출하면 상태 전이와 UI 업데이트를 정확히 테스트할 수 있다.  \n- **Promise‑throw 상황 디버깅**  \n  - React DevTools → Profiler 탭에서 `Suspended` 라벨이 붙은 Fiber 를 확인한다.  \n  - 콘솔에 `Unhandled Promise Rejection` 이 뜨면 가장 가까운 `<Suspense>` 경계가 없음을 의미한다.  \n- **Hooks 상태 확인**  \n  - `useTransition` 의 `isPending` 은 DevTools 의 **Hooks** 패널에서 실시간으로 확인 가능하다.  \n\n## 10. 도구·생태계 지원 현황\n- **React 18 공식 릴리즈 노트**: Concurrent Rendering 기본 활성화, `useTransition`, `useDeferredValue`, `useSyncExternalStore` 소개. ([React Blog – 18 Release](https://reactjs.org/blog/2022/03/08/react-18.html))  \n- **주요 UI 라이브러리 호환성**  \n  - **React Router v6.4+**: `loader` 와 `useLoaderData` 로 선언형 데이터 페칭을 구현하고, Suspense와 자연스럽게 결합한다.  \n  - **Redux Toolkit**: `useSyncExternalStore` 로 Redux 스토어를 구독하면 SSR에서도 일관된 상태를 유지한다.  \n  - **React Query / SWR**: 이미 캐싱·재시도·백그라운드 업데이트를 제공하므로, React 18의 Suspense와 병행 사용이 가능하다.  \n- **타입 정의**  \n  - `@types/react` 최신 버전에서 `useTransition`, `useDeferredValue`, `useSyncExternalStore` 의 타입이 포함되어 있다.  \n\n## 11. 결론 및 향후 로드맵\n- **생산성 향상 요약**  \n  - `useTransition`·`useDeferredValue` 로 UI와 데이터 로딩을 분리하면 사용자 경험이 부드러워지고, 코드 복잡도가 감소한다.  \n  - Suspense 경계를 적절히 설계하면 로딩 UI 관리가 일관되고, 서버‑사이드 렌더링과도 원활히 동작한다.  \n- **예정된 기능**  \n  - **React Cache 안정화**: 공식 API 로 전환될 경우, 선언형 데이터 페칭이 더욱 간결해진다.  \n  - **서버 액션 확장**: `useActionState` 와 유사한 서버‑사이드 액션 Hook이 정식 출시될 가능성이 있다.  \n- **조직 차원의 도입 전략**  \n  1. 파일 단위 파일럿 프로젝트에서 `useTransition`·`Suspense` 적용 후 성능 및 개발 속도 측정.  \n  2. 내부 가이드라인 문서화 및 워크숍 진행.  \n  3. 전체 코드베이스를 React 18+ 로 업그레이드하고, CI 파이프라인에 **Suspense 테스트**와 **SSR 일관성 검사**를 추가.  \n\n---  \n\n*본 문서는 React 공식 문서와 최신 커뮤니티 자료를 기반으로 작성되었습니다. 최신 API 변경 사항은 언제든지 React 공식 블로그와 릴리즈 노트를 확인하시기 바랍니다.*",
  "lastModified": "2026-03-20T06:21:11Z",
  "author": "SEPilot AI",
  "status": "draft",
  "isDraft": true,
  "isInvalid": false,
  "tags": [
    "React",
    "Concurrent Rendering",
    "Suspense",
    "useTransition",
    "useDeferredValue",
    "마이그레이션"
  ],
  "history": [
    {
      "sha": "87f3405",
      "message": "chore: Issue Processor 실행 결과",
      "author": "GitHub Action",
      "authorEmail": "action@github.com",
      "date": "2026-03-20T06:21:11Z",
      "isAutoCommit": false,
      "additions": 0,
      "deletions": 0
    }
  ]
}