name: Document Freshness Check

on:
  # 매주 일요일 UTC 0시 (KST 9시)
  schedule:
    - cron: '0 0 * * 0'

  workflow_dispatch:
    inputs:
      dry_run:
        description: '테스트 모드 (변경 적용 안함)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: doc-freshness-check
  cancel-in-progress: false

jobs:
  check-freshness:
    runs-on: jhl-space
    timeout-minutes: 15
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run freshness check
        id: freshness
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/check-freshness.js

      - name: Build wiki data
        if: steps.freshness.outputs.updated_count != '0'
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push changes
        if: steps.freshness.outputs.updated_count != '0'
        env:
          HUSKY: 0
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🔍 문서 최신성 점검: ${{ steps.freshness.outputs.outdated_count }}개 outdated 발견

          점검: ${{ steps.freshness.outputs.total_checked }}개 문서
          Outdated: ${{ steps.freshness.outputs.outdated_count }}개
          긴급: ${{ steps.freshness.outputs.critical_count }}개
          업데이트: ${{ steps.freshness.outputs.updated_count }}개

          🤖 Generated by Document Freshness Check"
            git pull --rebase
            git push
          fi

      - name: Job summary
        run: |
          echo "## 🔍 문서 최신성 점검 결과" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 결과 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 전체 문서 | ${{ steps.freshness.outputs.total_checked || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| Outdated | ${{ steps.freshness.outputs.outdated_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| 긴급 업데이트 | ${{ steps.freshness.outputs.critical_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| frontmatter 업데이트 | ${{ steps.freshness.outputs.updated_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
