name: Document Coverage Analysis

on:
  # 매월 15일 UTC 2시 (KST 11시)
  schedule:
    - cron: '0 2 15 * *'

  workflow_dispatch:
    inputs:
      dry_run:
        description: '테스트 모드 (변경 적용 안함)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: doc-coverage-analysis
  cancel-in-progress: false

jobs:
  analyze-coverage:
    runs-on: jhl-space
    timeout-minutes: 15
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Analyze coverage
        id: coverage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/analyze-coverage.js

      - name: Commit report
        env:
          HUSKY: 0
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 문서 커버리지 분석: ${{ steps.coverage.outputs.coverage_score }}점

          미문서화: ${{ steps.coverage.outputs.undocumented_count }}개
          긴급: ${{ steps.coverage.outputs.high_importance_count }}개

          🤖 Generated by Document Coverage Analysis"
            git pull --rebase
            git push
          fi

      - name: Job summary
        run: |
          echo "## 📊 문서 커버리지 분석 결과" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 결과 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 커버리지 점수 | ${{ steps.coverage.outputs.coverage_score || '0' }}/100 |" >> $GITHUB_STEP_SUMMARY
          echo "| 미문서화 모듈 | ${{ steps.coverage.outputs.undocumented_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| 긴급 문서화 필요 | ${{ steps.coverage.outputs.high_importance_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
