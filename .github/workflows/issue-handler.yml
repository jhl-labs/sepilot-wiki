name: Issue Handler

on:
  issues:
    types: [labeled, unlabeled, closed]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  handle-request-label:
    if: github.event.action == 'labeled' && github.event.label.name == 'request'
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Notify start
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '📝 요청을 확인했습니다. AI가 문서 초안을 작성 중입니다...'
            })

      - name: Generate document with AI
        id: generate
        run: |
          echo "🤖 AI 문서 생성 시작..."
          node scripts/generate-document.js \
            --issue-number "${{ github.event.issue.number }}" \
            --issue-title "${{ github.event.issue.title }}" \
            --issue-body "${{ github.event.issue.body }}"
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}

      - name: Build wiki data
        run: |
          npm run build:wiki
          npm run build:search

      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
          else
            git commit -m "docs: Issue #${{ github.event.issue.number }} - ${{ github.event.issue.title }}"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Add labels
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['draft', 'ai-generated']
            });

      - name: Comment with result
        uses: actions/github-script@v7
        with:
          script: |
            const issueTitle = context.payload.issue.title;
            const slug = issueTitle
              .toLowerCase()
              .replace(/[^a-z0-9가-힣\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .trim()
              .slice(0, 50);

            const body = [
              '✅ 문서 초안이 생성되었습니다!',
              '',
              '📄 **문서 위치**: `wiki/' + slug + '.md`',
              '🔗 **미리보기**: [문서 보기](https://' + context.repo.owner + '.github.io/' + context.repo.repo + '/wiki/' + slug + ')',
              '',
              '---',
              '',
              '이 문서는 AI가 자동으로 생성한 초안입니다.',
              '- 검토가 필요한 부분이 있다면 댓글로 피드백을 남겨주세요.',
              '- 문서가 완성되면 이 Issue를 닫아주세요.'
            ].join('\n');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  handle-invalid-label:
    if: github.event.action == 'labeled' && github.event.label.name == 'invalid'
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Mark document as invalid
        run: |
          echo "Issue #${{ github.event.issue.number }} - Invalid 라벨 감지"
          # 문서에 경고 표시 추가 로직
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ 이 문서에 오류가 보고되었습니다. 검토 후 수정하겠습니다.'
            });

  handle-issue-close:
    if: github.event.action == 'closed'
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Finalize document
        run: |
          echo "Issue #${{ github.event.issue.number }} - 종료됨"
          # draft 라벨 제거, 정식 문서로 전환
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Remove draft label
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: 'draft'
              });
            } catch (e) {
              console.log('draft 라벨이 없거나 제거 실패:', e.message);
            }

  handle-maintainer-comment:
    if: github.event_name == 'issue_comment'
    runs-on: jhl-space
    steps:
      - name: Check if maintainer
        id: check-maintainer
        uses: actions/github-script@v7
        with:
          script: |
            const { data: collaborators } = await github.rest.repos.listCollaborators({
              owner: context.repo.owner,
              repo: context.repo.repo,
            });
            const isMaintainer = collaborators.some(
              c => c.login === context.payload.comment.user.login &&
                   (c.permissions.admin || c.permissions.maintain)
            );
            return isMaintainer;

      - name: Checkout
        if: steps.check-maintainer.outputs.result == 'true'
        uses: actions/checkout@v4

      - name: Setup Bun
        if: steps.check-maintainer.outputs.result == 'true'
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        if: steps.check-maintainer.outputs.result == 'true'
        run: bun install

      - name: Process maintainer feedback
        if: steps.check-maintainer.outputs.result == 'true'
        run: |
          echo "Maintainer 피드백 처리"
          echo "댓글 작성자: ${{ github.event.comment.user.login }}"
          echo "댓글 내용: ${{ github.event.comment.body }}"
          # AI를 통한 피드백 반영 로직 (추후 구현)
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
