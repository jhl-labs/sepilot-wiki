name: Issue Handler

on:
  issues:
    types: [labeled, unlabeled, closed]
  issue_comment:
    types: [created]
  # push 이벤트는 워크플로우 검증용으로만 사용 (실제 동작 없음)
  push:
    paths:
      - '.github/workflows/issue-handler.yml'

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write

jobs:
  # push 이벤트일 때 워크플로우 검증용 (실제 동작 없음)
  validate-workflow:
    if: github.event_name == 'push'
    runs-on: jhl-space
    steps:
      - name: Workflow validation
        run: echo "✅ issue-handler.yml 워크플로우 검증 완료"

  handle-request-label:
    if: github.event.action == 'labeled' && github.event.label.name == 'request'
    runs-on: jhl-space
    outputs:
      has_changes: ${{ steps.commit.outputs.has_changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Notify start
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '📝 요청을 확인했습니다. AI가 문서 초안을 작성 중입니다...'
            })

      - name: Generate document with AI
        id: generate
        run: |
          echo "🤖 AI 문서 생성 시작..."
          bun run scripts/generate-document.js \
            --issue-number "${{ github.event.issue.number }}" \
            --issue-title "${{ github.event.issue.title }}" \
            --issue-body "${{ github.event.issue.body }}"
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Build wiki data
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push
        id: commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            git commit -m "docs: Issue #${{ github.event.issue.number }} - ${{ github.event.issue.title }}"
            git push
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Add labels
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['draft', 'ai-generated']
            });

      - name: Comment with result
        uses: actions/github-script@v7
        with:
          script: |
            const issueTitle = context.payload.issue.title;
            const slug = issueTitle
              .toLowerCase()
              .replace(/[^a-z0-9가-힣\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .trim()
              .slice(0, 50);

            const body = [
              '✅ 문서 초안이 생성되었습니다!',
              '',
              '📄 **문서 위치**: `wiki/' + slug + '.md`',
              '🔗 **미리보기**: [문서 보기](https://' + context.repo.owner + '.github.io/' + context.repo.repo + '/wiki/' + slug + ')',
              '',
              '> ⏳ **참고**: 배포가 진행 중입니다. 링크가 활성화되기까지 1~2분 정도 소요될 수 있습니다.',
              '',
              '---',
              '',
              '이 문서는 AI가 자동으로 생성한 초안입니다.',
              '- 검토가 필요한 부분이 있다면 댓글로 피드백을 남겨주세요.',
              '- 문서가 완성되면 이 Issue를 닫아주세요.'
            ].join('\n');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  handle-invalid-label:
    if: github.event.action == 'labeled' && github.event.label.name == 'invalid'
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Notify start
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ 오류가 보고되었습니다. AI가 문서를 검토하고 수정 중입니다...'
            })

      - name: Process invalid label with AI
        id: process
        run: |
          echo "🤖 Invalid 처리 시작..."
          bun run scripts/mark-invalid.js \
            --issue-number "${{ github.event.issue.number }}" \
            --issue-title "${{ github.event.issue.title }}" \
            --issue-body "${{ github.event.issue.body }}"
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Build wiki data
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push
        id: commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            git commit -m "fix: Issue #${{ github.event.issue.number }} - 오류 수정"
            git push
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment with result
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ 문서가 수정되었습니다. 검토 후 문제가 해결되었다면 invalid 라벨을 제거해주세요.'
            });

  handle-issue-close:
    if: github.event.action == 'closed'
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Notify start
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '📤 Issue가 종료되었습니다. 문서를 발행 상태로 전환 중입니다...'
            })

      - name: Publish document
        id: publish
        run: |
          echo "📤 문서 발행 시작..."
          bun run scripts/publish-document.js \
            --issue-number "${{ github.event.issue.number }}" \
            --issue-title "${{ github.event.issue.title }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Build wiki data
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push
        id: commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            git commit -m "docs: Issue #${{ github.event.issue.number }} - 문서 발행"
            git push
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Remove draft label
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: 'draft'
              });
            } catch (e) {
              console.log('draft 라벨이 없거나 제거 실패:', e.message);
            }

      - name: Add published label
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['published']
            });

      - name: Comment with result
        uses: actions/github-script@v7
        with:
          script: |
            const issueTitle = context.payload.issue.title;
            const slug = issueTitle
              .toLowerCase()
              .replace(/[^a-z0-9가-힣\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .trim()
              .slice(0, 50);

            const body = [
              '✅ 문서가 발행되었습니다!',
              '',
              '🔗 **발행된 문서**: [문서 보기](https://' + context.repo.owner + '.github.io/' + context.repo.repo + '/wiki/' + slug + ')',
              '',
              '> ⏳ **참고**: 배포가 진행 중입니다. 변경 사항이 반영되기까지 1~2분 정도 소요될 수 있습니다.',
              '',
              '---',
              '',
              '이 문서는 이제 정식 발행 상태입니다. 감사합니다! 🎉'
            ].join('\n');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  handle-maintainer-comment:
    if: github.event_name == 'issue_comment'
    runs-on: jhl-space
    steps:
      - name: Check if maintainer or owner
        id: check-maintainer
        uses: actions/github-script@v7
        with:
          script: |
            const commentUser = context.payload.comment.user.login;
            console.log(`댓글 작성자: ${commentUser}`);

            try {
              // 사용자의 repo 권한 레벨 직접 확인
              const { data: permissionData } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: commentUser,
              });

              const permission = permissionData.permission;
              console.log(`${commentUser}의 권한 레벨: ${permission}`);

              // admin 또는 maintain 권한이면 true
              const hasPermission = permission === 'admin' || permission === 'maintain';
              console.log(`maintainer 권한 여부: ${hasPermission}`);
              return hasPermission;
            } catch (error) {
              console.log(`권한 확인 실패: ${error.message}`);
              return false;
            }

      - name: Checkout
        if: steps.check-maintainer.outputs.result == 'true'
        uses: actions/checkout@v4

      - name: Setup Bun
        if: steps.check-maintainer.outputs.result == 'true'
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        if: steps.check-maintainer.outputs.result == 'true'
        run: bun install

      - name: Notify start
        if: steps.check-maintainer.outputs.result == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🔄 피드백을 확인했습니다. AI가 문서를 수정 중입니다...'
            })

      - name: Process maintainer feedback with AI
        if: steps.check-maintainer.outputs.result == 'true'
        id: process
        run: |
          echo "🤖 피드백 처리 시작..."
          bun run scripts/process-feedback.js \
            --issue-number "${{ github.event.issue.number }}" \
            --issue-title "${{ github.event.issue.title }}" \
            --comment-body "${{ github.event.comment.body }}"
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Build wiki data
        if: steps.check-maintainer.outputs.result == 'true'
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push
        if: steps.check-maintainer.outputs.result == 'true'
        id: commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            git commit -m "docs: Issue #${{ github.event.issue.number }} - 피드백 반영"
            git push
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment with result
        if: steps.check-maintainer.outputs.result == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ 피드백이 반영되었습니다. 변경 사항을 확인해주세요.'
            })

  # 모든 handle job이 완료된 후 배포 (하나라도 성공하면 실행)
  deploy:
    needs: [handle-request-label, handle-invalid-label, handle-issue-close, handle-maintainer-comment]
    if: |
      always() &&
      !cancelled() &&
      (
        needs.handle-request-label.result == 'success' ||
        needs.handle-invalid-label.result == 'success' ||
        needs.handle-issue-close.result == 'success' ||
        needs.handle-maintainer-comment.result == 'success'
      )
    runs-on: jhl-space
    steps:
      - name: Trigger deploy workflow
        uses: actions/github-script@v7
        with:
          script: |
            console.log('🚀 배포 워크플로우 트리거...');
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy-pages.yml',
              ref: 'main'
            });
            console.log('✅ 배포 워크플로우가 시작되었습니다.');
