name: Wiki Status Report

on:
  # 매주 월요일 UTC 1시 (KST 10시)
  schedule:
    - cron: '0 1 * * 1'

  workflow_dispatch:
    inputs:
      dry_run:
        description: '테스트 모드 (변경 적용 안함)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: wiki-status-report
  cancel-in-progress: false

jobs:
  generate-report:
    runs-on: jhl-space
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Generate status report
        id: report
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/generate-status-report.js

      - name: Build wiki data
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push changes
        env:
          HUSKY: 0
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 주간 위키 상태 보고서 생성

          문서: ${{ steps.report.outputs.total_documents || '0' }}개
          AI 활동: ${{ steps.report.outputs.weekly_ai_actions || '0' }}건

          🤖 Generated by Wiki Status Report"
            git pull --rebase
            git push
          fi

      - name: Job summary
        run: |
          echo "## 📊 주간 위키 상태 보고서" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 결과 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 전체 문서 | ${{ steps.report.outputs.total_documents || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| 주간 AI 활동 | ${{ steps.report.outputs.weekly_ai_actions || '0' }}건 |" >> $GITHUB_STEP_SUMMARY
          echo "| 보고서 경로 | ${{ steps.report.outputs.report_path || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
