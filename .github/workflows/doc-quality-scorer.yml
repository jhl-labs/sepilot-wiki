name: Document Quality Scorer

on:
  # 매월 1일 UTC 2시 (KST 11시)
  schedule:
    - cron: '0 2 1 * *'

  workflow_dispatch:
    inputs:
      dry_run:
        description: '테스트 모드 (변경 적용 안함)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: doc-quality-scorer
  cancel-in-progress: false

jobs:
  score-quality:
    runs-on: jhl-space
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run quality scoring
        id: quality
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/score-quality.js

      - name: Build wiki data
        if: steps.quality.outputs.updated_count != '0'
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push changes
        if: steps.quality.outputs.updated_count != '0'
        env:
          HUSKY: 0
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 문서 품질 평가: 평균 ${{ steps.quality.outputs.average_score }}점

          평가: ${{ steps.quality.outputs.total_scored }}개 문서
          저품질: ${{ steps.quality.outputs.low_quality_count }}개

          🤖 Generated by Document Quality Scorer"
            git pull --rebase
            git push
          fi

      - name: Job summary
        run: |
          echo "## 📊 문서 품질 평가 결과" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 결과 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 평가 문서 | ${{ steps.quality.outputs.total_scored || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| 평균 점수 | ${{ steps.quality.outputs.average_score || '0' }}점 |" >> $GITHUB_STEP_SUMMARY
          echo "| 저품질 문서 | ${{ steps.quality.outputs.low_quality_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
