name: Wiki Tree Maintainer

on:
  # 주 1회 실행 (매주 월요일 UTC 0시 = KST 9시)
  schedule:
    - cron: '0 0 * * 1'

  # 수동 실행 지원
  workflow_dispatch:
    inputs:
      dry_run:
        description: '테스트 모드 (변경 적용 안함)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      force_run:
        description: '변경 없어도 강제 실행'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  maintain-wiki-tree:
    runs-on: jhl-space
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run Wiki Tree Maintainer
        id: maintain
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/maintain-wiki-tree.js

      - name: Normalize tags
        id: tags
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
        run: bun scripts/maintenance/normalize-tags.js

      - name: Build wiki data
        if: steps.maintain.outputs.has_changes == 'true' || steps.tags.outputs.has_changes == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bun run build:wiki
          bun run build:search

      - name: Commit and push changes
        if: steps.maintain.outputs.has_changes == 'true' || steps.tags.outputs.has_changes == 'true'
        env:
          HUSKY: 0
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🌳 Wiki Tree Maintenance: ${{ steps.maintain.outputs.summary }}

          Applied: ${{ steps.maintain.outputs.applied_count }} changes
          Skipped: ${{ steps.maintain.outputs.skipped_count }} changes
          Issues: ${{ steps.maintain.outputs.issues_created }} created

          🤖 Generated by Wiki Tree Maintainer"

            git pull --rebase
            git push
          fi

      - name: Trigger deploy
        if: steps.maintain.outputs.has_changes == 'true' || steps.tags.outputs.has_changes == 'true'
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          event-type: wiki-tree-updated

      - name: Job summary
        run: |
          echo "## 🌳 Wiki Tree Maintainer 결과" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 결과 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 변경 사항 | ${{ steps.maintain.outputs.has_changes }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 자동 적용 | ${{ steps.maintain.outputs.applied_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| 보류 | ${{ steps.maintain.outputs.skipped_count || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "| Issue 생성 | ${{ steps.maintain.outputs.issues_created || '0' }}개 |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 요약" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.maintain.outputs.summary || 'Wiki 구조 분석 완료' }}" >> $GITHUB_STEP_SUMMARY
