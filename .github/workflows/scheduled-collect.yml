name: Scheduled Status Collection

on:
  schedule:
    # 매일 오전 9시 (KST) = UTC 0시
    - cron: '0 0 * * *'
    # 매 6시간마다
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      target:
        description: '수집 대상'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - actions

permissions:
  contents: write
  actions: read

jobs:
  collect-actions-status:
    runs-on: jhl-space
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Collect GitHub Actions status
        run: |
          echo "🚀 GitHub Actions 상태 수집 시작: $(date '+%Y-%m-%d %H:%M:%S KST')"
          bun run scripts/collect-actions-status.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Rebuild wiki data and search index
        run: |
          echo "📚 Wiki 데이터 및 검색 인덱스 재빌드..."
          bun run build:wiki
          bun run build:search

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          if git diff --staged --quiet; then
            echo "변경 사항 없음"
          else
            git commit -m "chore: 자동 상태 수집 - $(date '+%Y-%m-%d %H:%M')"
            git pull --rebase
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HUSKY: 0

      - name: Summary
        run: |
          echo "## 상태 수집 완료 ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 수집 시간: $(date '+%Y-%m-%d %H:%M:%S KST')" >> $GITHUB_STEP_SUMMARY
          echo "- 대상: ${{ github.event.inputs.target || 'all' }}" >> $GITHUB_STEP_SUMMARY
          if [ -f public/actions-status.json ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### GitHub Actions 상태" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
            cat public/actions-status.json | head -30 >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi
