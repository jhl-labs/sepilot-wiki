# SEPilot Wiki - Docker 환경변수 설정
# docker-compose 실행 시 자동으로 로드됨
#
# 사용법:
#   cp docker/.env.docker docker/.env
#   # 필요한 값 수정
#   docker-compose -f docker/docker-compose.dev.yml up --build

# ===========================================
# GitHub 설정 (선택사항)
# ===========================================
# GitHub API 토큰 - Issues, Actions 상태 조회에 사용
# 토큰 생성: https://github.com/settings/tokens
GITHUB_TOKEN=

# 대상 저장소 (owner/repo 형식)
GITHUB_REPO=

# Webhook 시크릿 (서버 모드에서 실시간 이벤트 수신 시 필요)
GITHUB_WEBHOOK_SECRET=

# ===========================================
# 인증 설정 (Keycloak 사용 시)
# ===========================================
# docker-compose.yml (private 모드) 사용 시에만 필요
# docker-compose.dev.yml은 public 모드로 인증 불필요

# KEYCLOAK_CLIENT_ID=sepilot-wiki
# KEYCLOAK_CLIENT_SECRET=
# KEYCLOAK_ISSUER=http://localhost:8080/realms/master
# NEXTAUTH_SECRET=your-secret-key
